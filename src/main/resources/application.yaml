server:
  port: 8080 # ${PORT:9000}
  servlet:
    context-path: /api

spring:
  spring:
    datasource:
      url: "jdbc:mysql://localhost:3306/QuanLyDonHang"
      driverClassName: "com.mysql.cj.jdbc.Driver"
      username: root
      password: 12345678
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
