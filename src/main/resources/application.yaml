server:
  port: ${PORT:9000}
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
    username: postgres.jdogabuoifwjfvblnnld
    password: 123456
    driver-class-name: org.postgresql.Driver  # 👈 thiếu dòng này

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  # 👈 thêm dòng này